/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["shots.codepen.io", "assets.codepen.io"],
    },
};

module.exports = nextConfig;
